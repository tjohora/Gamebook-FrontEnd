import { FilterPostIDPipe } from './filter-post-id.pipe';

describe('FilterPostIDPipe', () => {
  it('create an instance', () => {
    const pipe = new FilterPostIDPipe();
    expect(pipe).toBeTruthy();
  });
});
