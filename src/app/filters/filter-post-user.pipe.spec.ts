import { FilterPostUserPipe } from './filter-post-user.pipe';

describe('FilterPostUserPipe', () => {
  it('create an instance', () => {
    const pipe = new FilterPostUserPipe();
    expect(pipe).toBeTruthy();
  });
});
