import { FilterCommentIDPipe } from './filter-comment-id.pipe';

describe('FilterCommentIDPipe', () => {
  it('create an instance', () => {
    const pipe = new FilterCommentIDPipe();
    expect(pipe).toBeTruthy();
  });
});
