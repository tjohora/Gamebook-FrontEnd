import { FilterCommentsPipe } from './filter-comments.pipe';

describe('FilterCommentsPipe', () => {
  it('create an instance', () => {
    const pipe = new FilterCommentsPipe();
    expect(pipe).toBeTruthy();
  });
});
