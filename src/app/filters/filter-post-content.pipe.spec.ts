import { FilterPostContentPipe } from './filter-post-content.pipe';

describe('FilterPostContentPipe', () => {
  it('create an instance', () => {
    const pipe = new FilterPostContentPipe();
    expect(pipe).toBeTruthy();
  });
});
